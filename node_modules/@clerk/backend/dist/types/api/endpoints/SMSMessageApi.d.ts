import type { SMSMessage } from '../resources/SMSMessage';
import { AbstractAPI } from './AbstractApi';
type SMSParams = {
    phoneNumberId: string;
    message: string;
};
/**
 * @deprecated This endpoint is no longer available and the function will be removed in the next major version.
 */
export declare class SMSMessageAPI extends AbstractAPI {
    /**
     * @deprecated This endpoint is no longer available and the function will be removed in the next major version.
     */
    createSMSMessage(params: SMSParams): Promise<SMSMessage>;
}
export {};
//# sourceMappingURL=SMSMessageApi.d.ts.map