import { F as FileRouter } from './types-caf29eb6.js';
import { Json, UploadThingError } from '@uploadthing/shared';

type RouterWithConfig<TRouter extends FileRouter> = {
    router: TRouter;
    config?: {
        callbackUrl?: string;
        uploadthingId?: string;
        uploadthingSecret?: string;
    };
};

type CreateBuilderOptions<TErrorShape extends Json> = {
    errorFormatter: (err: UploadThingError) => TErrorShape;
};

export { CreateBuilderOptions as C, RouterWithConfig as R };
