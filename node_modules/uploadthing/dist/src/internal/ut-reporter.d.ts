import type { ActionType, UTEvents } from "./types";
export declare const createAPIRequestUrl: (config: {
    /**
     * URL to the UploadThing API endpoint
     * @example URL { /api/uploadthing }
     * @example URL { https://www.example.com/api/uploadthing }
     */
    url: URL;
    slug: string;
    actionType: ActionType;
}) => URL;
/**
 * Creates a "client" for reporting events to the UploadThing server via the user's API endpoint.
 * Events are handled in "./handler.ts starting at L200"
 */
export declare const createUTReporter: (cfg: {
    url: URL;
    endpoint: string;
}) => <TEvent extends keyof UTEvents>(type: TEvent, payload: UTEvents[TEvent]) => Promise<boolean>;
//# sourceMappingURL=ut-reporter.d.ts.map